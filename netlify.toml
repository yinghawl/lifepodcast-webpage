[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[context.deploy-preview]
  command = "npm run build:preview"

[dev]
  framework = "hugo"
  targetPort = 3000
